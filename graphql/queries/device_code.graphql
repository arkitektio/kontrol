query DeviceCodeByCode($code: String!) {
  deviceCodeByCode(deviceCode: $code) {
    ...DeviceCode
  }
}

query ValidateDeviceCode($deviceCode: ID!, $hub: ID!, $code: String!) {
  validateDeviceCode(deviceCode: $deviceCode, hub: $hub, code: $code) {
    valid
    reason
    existingDevice {
      id
      name
    }
    mappings {
      key
      serviceInstance {
        id
        identifier
      }
    }
  }
}
