query LatestClientReport($id: ID!) {
  client(id: $id) {
    id
    name
    lastReportedAt
    release {
      version
      app {
        identifier
      }
    }
    device {
      name
    }
    latestReport {
      ...ListReport
    }
    lastHealthyReport {
      ...ListReport
    }
  }
}
