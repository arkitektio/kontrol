fragment Device on ManagementDevice {
  id
  name
  nodeId
  clients {
    ...ListClient
  }
  deviceGroups {
    ...ListDeviceGroup
  }
}

fragment ListDevice on ManagementDevice {
  id
  name
  nodeId
}
