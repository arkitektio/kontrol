fragment DetailRelease on ManagementRelease {
  id
  version
  logo {
    presignedUrl
  }
  app {
    ...ListApp
  }
  clients {
    ...ListClient
  }
}

fragment ListRelease on ManagementRelease {
  id
  version
  logo {
    presignedUrl
  }
  app {
    ...ListApp
  }
}
